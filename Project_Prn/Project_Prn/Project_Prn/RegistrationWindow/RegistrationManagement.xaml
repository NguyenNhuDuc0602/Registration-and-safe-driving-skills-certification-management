<Window x:Class="Project_Prn.RegistrationWindow.RegistrationManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project_Prn"
        mc:Ignorable="d"
        Title="RegistrationManagement" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#e0e0ff" Padding="10" CornerRadius="10" Margin="0,0,0,10">
            <TextBlock Text="REGISTRATION MANAGEMENT"
               FontSize="28"
               FontWeight="Bold"
               Foreground="DarkBlue"
               HorizontalAlignment="Center"
               TextAlignment="Center"/>
        </Border>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Center" VerticalAlignment="Top" Width="Auto">
            <StackPanel Width="300" Margin="0,0,30,0">
                <TextBlock Text="RegistrationId:"/>
                <TextBox x:Name="txtRegistrationId"/>

                <TextBlock Text="UserID:" Margin="0,10,0,0"/>
                <TextBox x:Name="txtUserID"/>

                <TextBlock Text="CourseId:" Margin="0,10,0,0"/>
                <TextBox x:Name="txtCourseId"/>
            </StackPanel>
            <StackPanel Width="300">
                <TextBlock Text="Status:"/>
                <ComboBox x:Name="cbStatus" SelectedIndex="0">
                    <ComboBoxItem Content="Pending"/>
                    <ComboBoxItem Content="Approved"/>
                    <ComboBoxItem Content="Rejected"/>
                </ComboBox>
                <TextBlock Text="Comments:" Margin="0,10,0,0"/>
                <TextBox x:Name="txtComments"/>
                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,20,0,0">
                    <Button Content="Add" Height="35" Width="50" Margin="5"/>
                    <Button Content="Update" Height="35" Width="50" Margin="5"/>
                    <Button Content="Delete" Height="35"  Width="50" Margin="5" />
                    <Button Content="Detail" Height="35"  Width="50"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
        <DataGrid x:Name="dgRegistration" Grid.Row="2" 
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="RegisID" Binding="{Binding RegistrationId}" Width="50"/>
                <DataGridTextColumn Header="UserName" Binding="{Binding User.FullName}" Width="*"/>
                <DataGridTextColumn Header="CourseName" Binding="{Binding Course.CourseName}" Width="2*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTextColumn Header="Comments" Binding="{Binding Comments}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>